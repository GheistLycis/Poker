import { HasPlayerChangedPipe } from './has-player-changed-pipe';

describe('HasPlayerChangedPipe', () => {
  it('create an instance', () => {
    const pipe = new HasPlayerChangedPipe();
    expect(pipe).toBeTruthy();
  });
});
