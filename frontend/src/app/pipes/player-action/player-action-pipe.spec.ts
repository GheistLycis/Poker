import { PlayerActionPipe } from './player-action-pipe';

describe('PlayerActionPipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerActionPipe();
    expect(pipe).toBeTruthy();
  });
});
